node_modules/
.next/
coverage/
.DS_Store
*.log
*.tsbuildinfo
.env*
!.env.example

supabase/.temp/

# QA scratch: browser-driven run output, server logs and whole copies of the
# tree. Vitest already refuses to collect tests from here; this keeps it out of
# commits and, because `vercel` falls back to .gitignore, out of deployment
# uploads too — some of it is production QA logging.
.qa/

.vercel

# Measurement input for scripts/signal-sensitivity.ts: ~110 instruments of daily
# bars, tens of megabytes, refetchable with `--refresh`. It is not a gate and
# nothing is compared against it, so it does not belong in history.
__golden__/corpus/
